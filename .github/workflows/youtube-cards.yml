name: GitHub Readme YouTube Cards
on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: DenverCoder1/github-readme-youtube-cards@main
        with:
          playlist_id: UCEY98zVls33dVkLik5hYN2g
          comment_tag_name: YOUTUBE-CARDS
          show_duration: false
